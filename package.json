{
  "name": "server",
  "version": "1.0.0",
  "main": "app.js",
  "engines": {
    "node": "20.x",
    "npm": "10.9.2"
  },
  "scripts": {
    "start": "node app.js",
    "build": "cd client && npm install && npm run build",
    "heroku-postbuild": "npm install --legacy-peer-deps && npm run build && mv client/build server/build"
  },
  "dependencies": {
    "redux": "^4.2.1",
    "@redux-devtools/extension": "^3.3.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.3",
    "morgan": "^1.10.0",
    "validator": "^13.12.0"
  }
}
